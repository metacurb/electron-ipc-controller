{
  "name": "@electron-ipc-bridge/example-basic",
  "version": "0.1.1",
  "private": true,
  "description": "Basic Electron example using electron-ipc-bridge",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc && copyfiles -u 1 \"src/renderer/**/*\" dist",
    "start": "pnpm build && electron .",
    "dev": "tsc -w & electron ."
  },
  "dependencies": {
    "@electron-ipc-bridge/core": "workspace:*"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^40.6.1",
    "typescript": "^5.9.3"
  }
}
