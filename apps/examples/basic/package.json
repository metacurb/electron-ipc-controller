{
  "name": "@electron-ipc-controller/example-basic",
  "version": "0.1.0",
  "private": true,
  "description": "Basic Electron example using electron-ipc-controller",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc && copyfiles -u 1 \"src/renderer/**/*\" dist",
    "start": "pnpm build && electron .",
    "dev": "tsc -w & electron ."
  },
  "dependencies": {
    "@electron-ipc-controller/core": "workspace:*"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^40.2.1",
    "typescript": "^5.7.0"
  }
}
